// Inherit the parent event
event_inherited();
// pick random Building (not same location as current location) as destination
// create a marker/flag for desired destination?
destination = [-1,-1]; // Position on grid of destination
depth = -1;